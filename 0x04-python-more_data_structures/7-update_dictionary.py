def update_dictionary(a_dictionary, key, value):
    """
    A function that replaces or adds to a disctionary
    
    Parameters:
    - a_dictionary: A given dict
    - key: the key
    - value: the value

    Returns:
    new-dict
    """
    a_dictionary[key] = value
    return a_dictionary